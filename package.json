{
  "name": "@jxburros/onyx-engine",
  "version": "0.2.0",
  "private": true,
  "description": "Onyx Engine - OpenAI-compatible model-serving binary for JX Runtime. This package exists to fetch the build's only external input (the pinned llama.cpp source tree) from the npm registry; the binary itself is built with CMake.",
  "license": "MIT",
  "scripts": {
    "build": "cmake -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build --target onyx-engine -j",
    "test": "bash scripts/smoke-test.sh"
  },
  "dependencies": {
    "@jxburros/llama-cpp-source": "0.3.0-b10711.g9723942ad"
  }
}
